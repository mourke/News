class Widget {
  int xpos;
  int ypos;
  int widgetHeight;
  int widgetWidth;
  color widgetColour, labelColour;


  Widget(int xpos, int ypos, int widgetHeight, int widgetWidth, color widgetColour, color labelColour ) {

    this.xpos = xpos;
    this.ypos = ypos;
    this.widgetHeight = widgetHeight;
    this.widgetWidth = widgetWidth;
    this.widgetColour = widgetColour;
    this.labelColour = labelColour;
  }


  void setup() {
    fill (widgetColour);
  }

  void draw() {
    stroke(widgetMouseOver(mouseX, mouseY));
    rect(xpos, ypos, widgetHeight, widgetWidth);
    if (mousePressed && widgetClicked(mouseX, mouseY)) {
      //Do something
    }
  }


  boolean widgetClicked(int mX, int mY) {
  
    if ((mX>xpos && mX < xpos+widgetWidth && mY >ypos && mY <ypos+widgetHeight)&&mousePressed) {
      return true;
    
    } else 
    return false;
  }
  color widgetMouseOver (int mX, int mY) {
    if (mX>xpos && mX < xpos+widgetWidth && mY >ypos && mY <ypos+widgetHeight) {
      return (255);
    } else 
    return (0);
  }
}
