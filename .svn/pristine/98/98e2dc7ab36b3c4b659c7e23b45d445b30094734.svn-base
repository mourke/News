/*
class DropDownMenu extends Widget {

  ArrayList<DropDownItem> descendents;
  boolean selected;

  public DropDownMenu(int x, int y, int event, String label) {
    super(x, y, event, label);

    descendents = new ArrayList<DropDownItem>();
  }

  public DropDownMenu(int x, int y, int event, String label, DropDownItem ... args) {
    super(x, y, event, label);
    descendents = new ArrayList<DropDownItem>();
    for (DropDownItem w : args) {
      descendents.add(w);
    }
  }

  @Override
    public void draw() {
    if (selected) {
      fill( WIDGET_CLICKED);
    } else {
      fill(255);
    }
    rect(xpos(), ypos(), width(), height());
    fill(255, 0, 0);
    text(label(), xpos()+width()/5, ypos()+height()-height()/3);
    if (selected) {
      for (Widget w : descendents) {
        w.draw();
      }
    }
  }

  public void addDescendent(int event, String label) {
    descendents.add(new DropDownItem(this, descendents, event, label));
  }

  void select() {
    selected = true;
  }

  void deselect() {
    selected = false;
  }

  boolean isSelected() {
    return selected;
  }

  ArrayList<DropDownItem> descendents() {
    return descendents;
  }
}
*/
