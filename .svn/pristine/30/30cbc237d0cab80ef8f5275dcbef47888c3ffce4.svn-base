import  java.util.Date;

class Item {
  private ArrayList<Integer> kids;
  private Date releaseDate;
  private Integer id;
  private String author;
  private Type type;

  public Integer[] kids() {
    return kids.toArray(new Integer[kids.size()]);
  }

  public Date releaseDate() {
    return releaseDate;
  }

  public Integer id() {
    return id;
  }

  public String author() {
    return author;
  }

  public Type type() {
    return type;
  }


  Item(ArrayList<Integer> kids, 
    Date releaseDate, 
    Integer id, 
    String author, 
    Type type) {
    this.kids = kids;
    this.releaseDate = releaseDate;
    this.id = id;
    this.author = author;
    this.type = type;
  }
  
  Item(JSONObject dictionary) throws Exception {
    ArrayList<Integer> kids = new ArrayList<Integer>();
    JSONArray kidsArray = dictionary.getJsonArray("kids");
    for (int i = 0; i < kidsArray.size(); i++) {
      kids.add(Integer.valueOf(kidsArray.getInt(i)));
    }
    int unixTime = dictionary.getInt("time");
    Date releaseDate = new Date(unixTime);
    String author = dictionary.getString("by");
    Integer id = dictionary.getInt("id");
    String typeString = dictionary.getString("type");
    Type type;
    
    if (typeString.equals("story")) {
      type = Type.STORY;
    } else if (typeString.equals("comment")) {
      type = Type.COMMENT;
    } else {
      throw new Exception("Unreconised value in 'type' field");
    }
    
    this(kids, releaseDate, id, author, type);
  }
}
